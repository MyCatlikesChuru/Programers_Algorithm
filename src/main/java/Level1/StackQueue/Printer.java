package Level1.StackQueue;

import java.util.Arrays;

public class Printer {
    public static void main(String[] args) {
        Printer printer = new Printer();
        System.out.println(printer.solution(new int[]{2, 1, 3, 2},2)); // 1
        System.out.println(printer.solution(new int[]{1, 1, 9, 1, 1, 1},0)); // 5
    }
    public int solution(int[] priorities, int location) {
        int answer = 0;

        // A,B,C,D,E,F,G ...
        //

        // 문서 J에 대한 배열 방을 만든다. (우선순위 수량만큼)
        // 



        return answer;
    }
}
